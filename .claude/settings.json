{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.file_path' | { read -r f; case \"$f\" in *.java) head -n 30 \"$f\" | grep -q \"Licensed to the Apache Software Foundation\" || jq -n --arg f \"$f\" '{systemMessage: (\"Missing ASF license header: \" + $f)}'; ;; esac; } 2>/dev/null"
          }
        ]
      }
    ]
  }
}
